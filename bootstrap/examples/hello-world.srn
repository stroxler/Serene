(ns examples.hello-world)


(def hello-world
     (fn (name)
         (println "hello" name)))

(defmacro defn
  (name args &body)
  (list 'def name (cons 'fn (cons args body))))

(defn pp (x y)
  (println x))

(def main
     (fn (&args)
         (pp "sam")
         (hello-world "world")))
